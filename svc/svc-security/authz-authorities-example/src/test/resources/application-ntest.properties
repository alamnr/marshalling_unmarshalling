it.server.scheme = https
# must match self signed certificate from application-https.properties
it.server.trust-store = ${server.ssl.key-store}
it.server.trust-store-password = ${server.ssl.key-store-password}